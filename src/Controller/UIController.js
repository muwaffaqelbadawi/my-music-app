/* 

import songController from "./songController";
import albumController from "./albumController";

class UIController {
  constructor(songTitle, albumTitle) {
    this.songTitle = songTitle;
    this.albumTitle = albumTitle;
  }
}

const song = new songController("First song");
const album = new albumController();

const UI = new UIController(song.title, album.title);

 */